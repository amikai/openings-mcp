# Commit conventions

- Keep each PR to a single `companies.yaml` roster; batch all of a session's additions into one commit (squash if already split), titled `roster: add N companies to <Provider>` (first population) or `roster: enrich <Provider> companies` (existing).
