// Code generated by ogen, DO NOT EDIT.

package fourdayweek

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetJob implements getJob operation.
	//
	// Looks a job up by the `slug` a listing returned. The payload is exactly the same `Job` shape a
	// listing carries — this endpoint refreshes a single job, it does not enrich one, so there is no
	// reason to follow up a search with a detail call for extra fields.
	//
	// GET /api/v2/jobs/{slug}
	GetJob(ctx context.Context, params GetJobParams) (GetJobRes, error)
	// SearchJobs implements searchJobs operation.
	//
	// Paginated, filterable job listings. Every filter below is applied server-side — verified on
	// 2026-07-31, where the unfiltered board reported 22207 jobs, `work_arrangement=remote` narrowed it to
	// 6177, and adding `country=Germany` plus `schedule=4_day_week` narrowed it to 3.
	//
	// GET /api/v2/jobs
	SearchJobs(ctx context.Context, params SearchJobsParams) (SearchJobsRes, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
