// Code generated by ogen, DO NOT EDIT.

package nvidia

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetJobDetail implements getJobDetail operation.
	//
	// Fetches a single job posting. `externalPath` returned from the search endpoint always has the form
	// `/job/{location}/{titleSlug}` (exactly two segments after `/job/`); split it on the first `/` to get
	// these two path parameters. A single combined path parameter does not work here — standard URI
	// encoders percent-encode the `/` inside it, producing an invalid URL that the server rejects with
	// HTTP 400.
	//
	// GET /job/{location}/{titleSlug}
	GetJobDetail(ctx context.Context, params GetJobDetailParams) (GetJobDetailRes, error)
	// SearchJobs implements searchJobs operation.
	//
	// Searches jobs by keyword and/or facet filters, paginated via `limit`/`offset`. `limit` is capped
	// server-side at 20; a higher value returns HTTP 400. Observed minimal request requirements:
	// `appliedFacets`, `limit`, `offset`, and `searchText` are all required by the API (send `{}` for
	// `appliedFacets` and `""` for `searchText` when unused).
	//
	// POST /jobs
	SearchJobs(ctx context.Context, req *JobsRequest) (SearchJobsRes, error)
	// NewError creates *ErrorResponseStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrorResponseStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
