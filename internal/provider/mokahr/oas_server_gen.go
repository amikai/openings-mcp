// Code generated by ogen, DO NOT EDIT.

package mokahr

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetJob implements getJob operation.
	//
	// Returns the fields the list endpoint omits, notably `department` and `commitment` (employment type).
	//
	// POST /api/outer/ats-apply/website/job
	GetJob(ctx context.Context, req *GetJobRequest) (*GetJobResponse, error)
	// ListFilterAggregations implements listFilterAggregations operation.
	//
	// Supplies the ids `listJobs` takes as `locationIds` and `zhinengIds`, together with their display
	// labels. Unlike the job endpoints, this one answers in the clear.
	//
	// POST /api/outer/ats-apply/website/jobs/v2/filterFieldsAggregations
	ListFilterAggregations(ctx context.Context, req *SiteRef) (*FilterAggregationsResponse, error)
	// ListJobs implements listJobs operation.
	//
	// Returns one page of open postings, each with its full HTML description. `needStat: true` adds
	// `jobStats.total`, the only source of an exact board size.
	//
	// POST /api/outer/ats-apply/website/jobs/v2
	ListJobs(ctx context.Context, req *ListJobsRequest) (*ListJobsResponse, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
