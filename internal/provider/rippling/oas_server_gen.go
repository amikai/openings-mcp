// Code generated by ogen, DO NOT EDIT.

package rippling

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetJob implements getJob operation.
	//
	// Retrieve a single job posting.
	//
	// GET /board/{board_slug}/jobs/{job_uuid}
	GetJob(ctx context.Context, params GetJobParams) (GetJobRes, error)
	// ListJobs implements listJobs operation.
	//
	// Full-board dump; no pagination or query parameters. Quirk: the array carries one entry per (job,
	// workLocation) pair, so a multi-location job repeats once per location under the same uuid —
	// consumers must dedupe by uuid and merge the per-entry workLocation values.
	//
	// GET /board/{board_slug}/jobs
	ListJobs(ctx context.Context, params ListJobsParams) (ListJobsRes, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
